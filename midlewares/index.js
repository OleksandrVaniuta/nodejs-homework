const validateBody = require('./validates');
const isValideId = require('./isValidate');
const authenticate = require('./authenticate');

module.exports = {
  validateBody,
  isValideId,
  authenticate,
};
